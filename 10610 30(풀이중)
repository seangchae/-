#include<cstdio>
#include<cstring>
#include<iostream>
#include<algorithm>

using namespace std;

bool cmp(int left, int right)
{
	return left > right;
}

int main()
{
	vecter <int>;
	scanf("%d", &n);
	sort(vector.begin(), vector.end(), cmp);
	if(vector % 30 == 0)
	{
		printf("%d", vector);
	}
	else
		printf(-1);
		
	return 0;
}
